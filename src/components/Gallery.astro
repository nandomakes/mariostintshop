---
import { INSTAGRAM } from '../consts';
import { getInstagramFeed } from '../lib/instagram';

// Pulls the shop's latest Instagram posts at build time (falls back to the
// curated images in consts.ts when no INSTAGRAM_TOKEN is configured).
const posts = await getInstagramFeed(6);

// Bento layout: columns 1–3 each stack two photos (varying split per column),
// column 4 is one full-height photo — 7 tiles total. We only have 6 real
// photos today, so tile 5 (col 3, bottom) reuses tile 4's photo as a
// placeholder until a 7th curated photo is added — swap it for a real one
// then and this duplication goes away.
const tiles = [...posts, posts[4]];
// 20-row grid so column 3's split can land at a moderate 45/55 — columns 1–2
// keep their original ~30/70 ratio (just rescaled onto the finer 20 rows).
const layout = [
  { col: 1, row: '1 / 7' },   // 0: col 1, short (top)   — 30%
  { col: 1, row: '7 / 21' },  // 1: col 1, tall (bottom)  — 70%
  { col: 2, row: '1 / 15' },  // 2: col 2, tall (top)     — 70%
  { col: 2, row: '15 / 21' }, // 3: col 2, short (bottom) — 30%
  { col: 3, row: '1 / 10' },  // 4: col 3, short (top)    — 45%
  { col: 4, row: '1 / 21' },  // 5: col 4, full height
  { col: 3, row: '10 / 21' }, // 6: col 3, tall (bottom, duplicate placeholder) — 55%
];
---

<section id="gallery" class="bg-ink">
  <div class="mx-auto max-w-6xl px-5 py-20 md:py-24">
    <div class="text-center">
      <p class="reveal kicker text-accent">Our latest work</p>
      <h2 class="reveal headline mx-auto mt-3 max-w-3xl text-[clamp(1.4rem,2.8vw,2.2rem)] text-paper">
        Where craftsmanship meets flawless finish
      </h2>
    </div>

    <div
      class="reveal showcase-grid mt-12 grid aspect-[3/4] grid-cols-2 [grid-template-rows:repeat(3,1fr)] gap-4 sm:aspect-auto sm:h-[420px] sm:grid-cols-4 sm:[grid-template-rows:repeat(20,1fr)] md:h-[560px]"
    >
      {
        tiles.map((img, i) => {
          const spot = layout[i];
          // The 7th tile only exists to fill the desktop bento; on mobile's
          // 2-column grid it would show as a visible duplicate plus an orphan
          // half-empty row, so it's dropped below sm.
          const isFiller = i === tiles.length - 1;
          return (
            <div
              class:list={['group relative overflow-hidden', isFiller && 'hidden sm:block']}
              style={`--col:${spot.col}; --row:${spot.row};`}
            >
              <img
                src={img.src}
                alt={img.alt}
                width="600"
                height="600"
                loading="lazy"
                decoding="async"
                draggable="false"
                class="h-full w-full object-cover transition-transform duration-700 ease-out group-hover:scale-[1.05]"
              />
            </div>
          );
        })
      }
    </div>

    <p class="reveal mt-8 text-center text-sm text-smoke">
      See our latest tint, PPF, and ceramic work —
      <a
        href={INSTAGRAM.url}
        target="_blank"
        rel="noopener noreferrer"
        class="font-bold text-accent underline underline-offset-4 transition-colors hover:text-accentDk"
      >
        {INSTAGRAM.handle}
      </a>
    </p>
  </div>
</section>

<style>
  /* Desktop bento: each photo's column/row span comes from its --col/--row
     custom properties (set inline per-item above). */
  @media (min-width: 640px) {
    .showcase-grid > div {
      grid-column: var(--col);
      grid-row: var(--row);
    }
  }
  /* Mobile: collapse to a plain 2-col grid of equal-height photos — the
     bento proportions only make sense once there's room for 4 columns. */
  @media (max-width: 639px) {
    .showcase-grid > div {
      grid-column: auto;
      grid-row: auto;
      height: 100%;
    }
  }
</style>
